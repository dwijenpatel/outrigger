# Distilled — internal evidence (our own runs)

Tier-A facts generated by this project about itself. Grading method: [README.md](README.md).
Sources: [../internal/](../internal/) (pilot ledgers, committed telemetry, the local benchmark),
plus the internally-sourced claims embedded in three mixed-provenance external documents, and
[`tools/budget-governor/probe-spawn-portability-2026-07-04.md`](../../../tools/budget-governor/probe-spawn-portability-2026-07-04.md).

**The governing asymmetry.** We want this design to work. Therefore:

> **Our recorded defects and falsified predictions are Tier-A evidence (A2, against interest).
> Our recorded wins are Tier C — claims — until a committed artifact backs them (A3).**

A win with a committed run-log line, verdict file, or gate stamp is checkable by a stranger with
a clone. A win that lives only in a pilot-clone transcript is an assertion. §5 grades them
honestly. *(Until 2026-07-11 the design's headline win failed this bar; the
[pilot-3 salvage](../internal/pilot-3-artifacts/README.md) brought its artifacts into the tree
and §5 row 3 now grades A3.)*

Pilot observation IDs collide across files (`P2-x` exists in both `pilot-1-observations.md` and
`pilot-2-observations.md`), so every citation names its file.

---

## 1. Predictions our own runs falsified `A2` — the highest-value evidence here

Each row is the design, or the operator, being wrong in a way that cost us something to record.

| We believed | Reality | Decay |
|---|---|---|
| §5.3, from vendor docs: per-agent `(model, effort)` overrides dispatch, and **"invalid ids fail loud."** | The third clause was **false**. An invalid `effort` string is **silently accepted** — no throw, no log, no-op. An invalid `model` id resolves `agent()` to **`null`** plus an async workflow-log line; a `try/catch` never fires. Spawn code must allowlist-validate `(model, effort)` and null-check results. | `vendor-build` (probed on 2.1.45) |
| Dispatch success implies the knob was **applied**. | **Acceptance ≠ application.** On the headless path (build 2.1.201), effort *is* applied: identical prompt, low→max moved output **109 → 5,947 tokens (~55×)**. On the `Agent`/`Task` path it is **not** — effort is session-inherited, so the ladder's effort rung was never actuatable there (P3v2-6). | `vendor-build` |
| A pilot clone may implement machinery fixes locally. | **P2-collision** (pilot-2 P2-5): parent and pilot session independently implemented I4; the sync resolved `vault.py` "to the parent" and destroyed the better version → broken chimera, **17 test errors**. *Two competent agents fixing the same defect produced a worse outcome than either alone.* Machinery is now upstream-owned. | `our-tree` (rule is durable) |
| Ratification guarantees an internally consistent instrument; `plan_ready` suffices. | A **ratified** plan was floor-inconsistent (D17 floors `models.py` at high; GL1 was routine and touched it) and `plan_ready` was blind to it, because predicted diff paths lived in spec prose. Discovered mid-firing, not at ratification. → I19 `gate_preflight`, `touches`. | `our-tree` (fixed) |
| Registration of a hook implies it executes. | **Falsified twice.** `$CLAUDE_PROJECT_DIR` is hooks-only, so the statusline shim's path expanded empty (pilot-2 P2-6). Then: the operator runs the **desktop app, which has no statusline surface at all** — correct machinery aimed at a nonexistent surface (pilot-2 P2-8). *"Registration checks prove presence, not execution."* | `vendor-build` |
| Substituting Opus for a quota-blocked Fable worker is safe — "Opus is unaffected." | **Premise false.** Under the same over-degrade window that 429'd Fable, Opus was throttled into a **38-minute, zero-byte, zero-progress stall** — same pressure, different failure shape. *Substitution does not dodge a window; parking does.* | `vendor-policy` |
| "Always-high effort overthinks trivial inputs (+1,953% tokens)" (external, arXiv 2412.21187) applies to our routing. | **Superseded on the adaptive-thinking lineup** by our own benchmark: easy tasks cost the same at *every* effort — the dial self-adjusts. Uniform `xhigh` is safe. The asymmetric half still holds: **always-low drops success** (Sonnet 5 wrong at low/medium where Opus and Fable at low were right). | `model-generation` |
| The I26 headless arm's own documented incantations work. | **Three falsified against real CLI 2.1.201 in one leg**: `--json-schema` parses its value as inline JSON, not a path (P3v2-9); a *compliant* worker yields `parsed=None` because the CLI leaves `structured_output=null` and the model fences its JSON (P3v2-10); `load_patterns` rejects the tuple the skill's own documented call passes (P3v2-11). | `vendor-build` |
| The recorded "8h25m stall" and "5-hour window reset unused mid-stall". | **Both artifacts of a ~+8h host-clock skew.** True wait ≲85 min. We corrected our own headline numbers downward. | — |

**Retractions we made against ourselves** (defect claims we withdrew after checking):
`P2-15` (pilot-2) — a suspected model-attribution bug "was a misread; per-attempt records were
exemplary." `P2-3` reviewer note (pilot-1) — "the initial review misread wrapped diff output as a
`./`-prefixed typo; the actual edit was correct. The class of error is real even though this
instance wasn't." A corpus that retracts its own findings is one whose surviving findings mean
more.

## 2. Defects found in our own machinery `A2`

All 🔴 entries, all fixed unless noted. The fix IDs are the plan's Phase-I increments.

| ID (file) | Defect | Fix |
|---|---|---|
| P1-5 (pilot-1) | `state/` not gitignored → the loop's own mandated state files dirty the tree → `require_clean` refuses **every** merge. | gitignore |
| P1-8 (pilot-1) | **No planning surface.** The loop improvised the plan from a one-line description. The entire O0 chain trusted a spec nothing mechanized producing. | I2 (`plan-build`) |
| P1-9 (pilot-1) | A tracked `.pyc` → `.gitignore` never applies to tracked files → any import rewrites it → gates refuse in every clone. | `git rm --cached` |
| P2-2 (pilot-1) | The ratified plan was **editable by the workers judged against it** (`plan/` absent from `MACHINERY_GLOBS`). | I6 |
| P2-2 (pilot-2) | Governor died with a raw `FileNotFoundError` on a missing statusline source instead of degrading. | I4b |
| P2-4 (pilot-2) | **Bootstrap deadlock.** No statusline dump + no OAuth doc + unpopulated estimates → occupancy `unknown` → admission refuses everything → *a first firing can never start a single task.* Two correct fail-closed rules composing into deadlock. | I4b (`--assume-occupancy`) |
| P2-5 (pilot-2) | **P2-collision** (see §1). | upstream-ownership rule |
| P2-6 / P2-8 (pilot-2) | Statusline shim never fired; then: no statusline surface exists in the desktop app. | I9b, I13 (OAuth rung) |
| P2-9 (pilot-2) | Worker worktree homes untracked → gates refuse. **Fourth instance** of the tree-dirt family. | gitignore |
| P2-10 (pilot-2) | **The gate never ran the held-out corpus.** The single most load-bearing O0 mechanism was orchestrator *prose*; leakage control rested on "the orchestrator's manners." | I14 (`vault.materialize`, `record_replay`) |
| P2-13 (pilot-2) | The routine step-manifest omitted `risk_floor` → the floor defense was **silently off exactly where mis-tagging is most likely**. | I16 (floors-always) |
| P3-2 (pilot-3) | Ratified plan internally floor-inconsistent; systemic across GL4/6/7/10. | I19 |
| P3v2-1 (pilot-3) | A profile raise silently armed H9 → 10 carried-over ambiguities fired pre-spawn → interactive ask stalled the firing. Four layered causes, including an **ask-before-card write-ahead violation**. | I19, I20, I21 |
| P3v2-2 (pilot-3) | `git_guard` false positive: a destructive-pattern gap class matched **across a newline**, from `echo "clean"` to a later `-ref`. | I22 |
| P3v2-3 (pilot-3) | `fraction_rate`'s cross-firing lookback pulled a prior firing's reading, engaging reset-headroom on a fresh firing. | I25 (firing-scoped burn) |
| P3v2-6 (pilot-3) | The escalation ladder's effort rung **is not actuatable** on the `Agent`/`Task` spawn path. | I24 |
| P3v2-8 (pilot-3) | Pause channel starved ~17 min behind a **foreground** worker. | I23 (flag + background + polling) |
| P3v2-9/-10/-11 (pilot-3) | Three CLI-contract breaks in one leg (see §1). | I29 |
| P3v2-12 (pilot-3) | Fable 5 hit a hard **429 "out of usage credits" on its first call, 0 tokens** — a model-specific weekly cap far below the general windows and **invisible to the statusline rung**. | I28 (Fable removed; classify pattern) |
| P3v2-13 (pilot-3) | Opus substitute **hung ~38 min, 0 bytes out**. No client wall-clock watchdog; no heartbeat. | I29 deadline. **Liveness ping still OPEN.** |

**In-tree self-audit** `[in-tree 2026-07-04]`, against our own interest: **no settings file
registered any hook** (the safety scripts were deterministic once invoked, but nothing made them
un-skippable); **the triggers were prose** (nothing intercepted a merge to demand a PASS report);
**the gate was skippable-by-omission** (every absent input yielded a passing "caller's choice"
step); **the governor had no staleness handling.** Fixed in Phase H.

**Concurrency source audit** `[code-read 2026-07-10]`, from the orchestration pass — discovered
by reading `harness/ledger.py` and `harness/interlocks.py`, not by a firing. **All OPEN** (the
repo is in research mode; machinery is upstream-owned): recorded here and in the
[concurrency doc's hardening backlog](../external/orchestration/concurrency-and-merge-correctness.md),
deliberately not fixed. These give the pre-registered watch items W1–W3 (§3) concrete code-level
mechanisms while the items themselves remain unobserved live.

| ID (maps) | Defect | Status |
|---|---|---|
| **B-4** (W3) | **Gate-stamp base-move race — the one real correctness hole.** `_require_stamp` validates only the *source* ref HEAD; the stamp records `base` but **nothing checks `base` against the current target HEAD**. A and B both gate against `main@X`; A merges (`main→Y`); B's source HEAD is unchanged, so the interlock **admits B though it was only ever proven green against `main@X`** — textbook merge-skew. Exposed under true `git merge`; masked under rebase/FF. **Present at the minimum cap of 2; the `base` field the fix needs is already in the stamp.** | **OPEN — backlog #1** (bind stamp to `base`, invalidate on base-move; or mandate rebase-before-merge). The likeliest source of the harness's first *silent* wrong merge. |
| **B-1** (W1) | **OCC atomicity gap.** No lock spans read→validate→write (no `flock`/`fcntl`/`O_APPEND`/lock anywhere in `harness/`); two writers reading `generation=N` both pass validation, the second **clobbers** the first. Safe **only** because the shared log is single-writer — an architectural invariant no mechanism enforces. | OPEN — backlog #2 (advisory `flock`, or a selftest failing if any non-orchestrator path reaches `EventLog.append`). |
| **B-2** (W1) | `seq = len(events)+1` is SELECT-MAX+1 — collides under two writers, correct under one. Append-only monotonicity is the redeeming strength (ABA-immune). | OPEN — same root as B-1. |
| **B-3** (W2) | **No per-record checksum.** Torn-tail integrity is newline-boundary + JSON-parse + monotonic-seq, not checksum; a torn write landing valid JSON with a plausible `seq` would pass. Severity low (single ~4KB fsync'd lines). | OPEN, optional — per-record CRC only if records grow multi-line. |

Also convention-not-invariant: `reconcile` outranks the event log **only when the caller passes
`artifacts`** — the design's "reconstruct from artifacts before acting" rule is a convention no
type enforces. Warrant: A3 grep-verified + A2 against interest. Decay: `our-tree` — recheck on
any commit touching those files.

**The named theme.** P1-5, P1-9, P2-9, and the pilot-3 boundary-sync are **four instances of one
failure**: the machinery's own bookkeeping dirties the very tree its gate judges. Every one was a
composition defect; **no hermetic module test caught any of them.** This is the strongest empirical
argument in the corpus for the smoketest (I5) and against confidence-by-unit-test.

## 3. Pre-registered predictions and their outcomes

Pre-registration is what separates a prediction from hindsight. The 👁 glyph means "recorded
before observation," and `pilot-2-artifacts/watch-items.json` is **committed, dated
(`registered_at: 2026-07-05T08:38:24Z`), and acked** — a real pre-registration artifact.

| Watch item | Pre-registered | Outcome |
|---|---|---|
| **W1–W5** — the whole concurrency family (stale-generation, write-ahead under interleaving, gate-stamp/HEAD races, admission warmup, worktree conflicts) | ✅ committed + dated | **UNOBSERVED — no opportunity.** Concurrency never ran: GL1 was the serial root, GL2 parked. **2026-07-10 update:** the source audit (§2) gave W1/W2/W3 named code-level mechanisms (B-1/B-2, B-3, B-4) — the *predictions* stand, now with the exact lines that would realize them. |
| W6 — the held-out corpus catches what blind validation misses (**the design thesis**) | ✅ | **CONFIRMED once** (n=1). Artifacts committed 2026-07-11 (§5 row 3 → A3). |
| W8 — write-ahead `task_spawn` with resolved spawn params | ✅ | **CONFIRMED**, visible in the committed run-log. |
| W9 — the price of floors-always | ✅ | **CONFIRMED, n=1**: GL1 ≈ **260,659 tokens** across 2 attempts, **panel spend ≈ 63%**. |
| W7 — `fraction_rate` cross-firing lookback | ✅ | **CONFIRMED as a defect** → I25. |
| "GL10-seed will bounce at the gate under floors-always" | ✅ explicit, pilot-2 close | **FALSIFIED on specifics.** **GL1** bounced first, not GL10 — GL1 also creates floored paths. The *class* was right; the named task was wrong, and pilot-2's own tick-1 data already contained the disconfirming evidence but it "did not get propagated" (P3-3). |
| P2-1 (pilot-2) — can a worker `pip install` inside the sandbox? | ✅ | **UNOBSERVED.** The sandbox's dependency-drift bound is still unknown. |

Two of these are worth naming. The **GL10 miss** is a pre-registration that failed in a checkable
way — the good kind. And **W1–W5 unobserved** means the entire paid-parallelism and
admission-warmup apparatus has *zero* real telemetry.

## 4. Directly reproducible local measurements `A3`

### The model-speed/effort benchmark (2026-07-05) — qualifies as Tier A

**Why it qualifies:** the artifact is committed and git-tracked — **73 raw run JSONs** (round 1:
21, round 2: 16, round 3: 36) plus **9 harness files** (`run_bench*.sh`, `grade.py`,
`grade_hard.py`, fixtures). Each JSON is the CLI's own `--output-format json`. Total spend ≈ $15.
Strip the artifact and this would be a self-serving single-source measurement — Tier C. **The
artifact is the warrant.**

| Finding | Number | n |
|---|---|---|
| Correctness is **saturated** at prompt scale | **36/36** fully-correct runs, including Haiku on a regex engine | full round-3 sample |
| Speed ranking **inverts by regime** | Fable fastest wall-clock on thinking-heavy (GEN api_s **45** [40–75] vs Opus 74, Sonnet 84, Haiku 171) | n=3, median [min–max] |
| Token spend varies a lot between identical reps | up to **3×**; worst cell Opus/HARD **5.0k–15.3k tok, 55–190 s, $0.16–$0.42** | n=3 |
| Token efficiency offsets the price ladder | Opus ≈ Sonnet per solved task (1.08 / 0.95 / 1.40 vs Sonnet=1.0); Fable ≈ **1.6–1.8×** Sonnet on thinking-heavy work, not its 10× price multiple | derived from n=3 medians |
| **Effort-down is not a safe economy** | Sonnet 5 **wrong** at low and medium where Opus and Fable at low were **right** | n=1 per cell |
| Effort scaling low→max (tokens) | Sonnet **4.2×**, Opus **3.1×**, Fable **2.4×** | n=1 per cell |

**Stated caveats, kept:** round 2 is n=1 per cell; one machine, one day, one subscription account,
4–5 runs in parallel; timings include Claude Code's system prompt and cache writes (representative
of *our* usage, not of raw API calls); "correctness saturated" is a statement about **these task
sizes**, not model parity — the routing claims that depend on horizon-scale separation rest on
*external* benchmarks, not on this. Decay: `model-generation` — void on any new model in the lineup.

**Independent live corroboration** (the I26 probe, build 2.1.201, n=1 per rung): effort is applied
on the headless path (**109 → 5,947** output tokens, ~55×), and correctness *inverted* with effort
(sonnet@low wrong, sonnet@max right) — a live replication of "effort-down isn't safe." It also
measured cache behavior directly: same model+effort share the prefix cache (27k of 37.5k read);
**an effort change busts it** (0 read, 37.4k fresh).

### The tokenizer study (`tool-surface` §4.3) — **Tier B, not Tier A**

Real measurements (o200k_base via js-tiktoken, official TOON encoder, round-trip losslessness
verified, seeded on our own ledger/run-log shapes): flattened TOON **−48.0%** vs compact JSON,
CSV −55.0%, Markdown −36.7%; but raw list-valued ledger shapes **+16.8%**, nested verdicts
**+7.1%**, pretty JSON **+48–82%**.

It falls short of Tier A on its own stated grounds: o200k is a **proxy** ("Anthropic's tokenizer
differs — treat ratios, not absolutes"), and the harness was never committed — a search of the
surviving session scratchpads (2026-07-11, reincarnation step 1) **confirmed it is gone.** The
measurement is permanently non-promotable; if the question ever matters again, re-run from
scratch. Under this document's rule, a measurement without a committed artifact is a claim —
this row is now the standing example of why the rule exists.

Two things nonetheless raise confidence in it above a typical self-run number: the measurement is
**deterministic token counting**, with no model judgment anywhere in the loop; and its conclusion
runs **against the author's own initial lean** — the study was expected to justify adopting TOON
and instead produced the "No TOON" rule. An `A2` flavor inside a `Tier B` measurement.

## 5. Wins — graded by artifact, not by enthusiasm

| Win | What it shows | Artifact | Tier |
|---|---|---|---|
| **P2-14** (pilot-2) — tick 1 exercised the whole floor and it held: statusline rung live, blocker-card resume, write-ahead + generation stamps clean (7→10, no stale-gen), and **the I12 ladder ran textbook — haiku FAIL ×2 (effort rung between) → sonnet PASS. All three worker self-reported "pass" claims were falsified by ground truth *before* panel or gate spend.** | `claims, not evidence` earns its keep; the escalation ladder works | **Committed.** `run-log.jsonl` records the exact ladder (`haiku/cheap/low` a1 → `haiku/cheap/medium` a2 → `sonnet/standard/medium` a3 → merge); `governor-log.jsonl` shows assumed-0.3 → live-0.62; `verdicts/GL1-scaffold/correctness.json` is a committed PASS. | **A3** |
| **P3v2-12** (pilot-3) — the governor's degrade hold deferred GL2; the operator **overrode it**; the fresh worker died on its first call (429, 0 tokens, $0). | The governor was right, and we know because we disobeyed it | **Committed.** `run-log.jsonl` carries the `task_aborted` record; `governor-log.jsonl` the degrade reading. | **A3** — unusually strong: the disconfirming action was taken and the machinery still won. |
| **P3v2-5** (pilot-3) — **the design's central thesis, first live proof.** Attempt 1's `get_db(request)` passed visible tests, typecheck, *and* a full 3-lens Opus panel. The **held-out corpus failed 3 tests.** Attempt 2 (fresh worker, no leakage) fixed it and caught a latent `dependency_overrides` closure bug that would have silently broken later DB tasks. | The vault catches what blind validation alone misses | **Committed 2026-07-11** — [`pilot-3-artifacts/`](../internal/pilot-3-artifacts/README.md): the 3 Opus verdicts, the gate stamp, the 11-step gate report, the 7-file held-out corpus, the vault-side `41 passed` log, and the 4-replay meter. Caveat kept: the attempt-1 *failing* log was not retained vault-side — that link survives as the replay timestamps + two-wave run-log, not a preserved failure artifact. | **A3** (n=1) |
| P3-1 (pilot-3) — the floor collision was caught **statically, pre-spawn**, parked on a complete blocker card, zero worker spend. | I16 worked at the cheapest possible point | Clone/prose + the instrument-v2 ratification stamp. No telemetry (nothing spawned). | Tier C |
| P2-1 (pilot-1) — the `plan-build` interview performed as designed (~20 single-question rounds, D1–D24 decisions log, P1-8 did not recur). | The planning surface works | Clone `PLAN.md`, not committed here. | Tier C |
| Leg-2 start gates all green — smoketest **17/17** (first live use), plan-ready **7/7**, selftest **30/30**. | The gates run | Narrative only. (The suites themselves are of course re-runnable.) | Tier C |

**Row 3's history is worth keeping.** From 2026-07-05 to 2026-07-11 the single result this
design exists to produce — *the held-out vault caught a defect that visible tests and three
blind validators all missed* — was, in this repository, **an assertion**: its artifacts had
never been brought back from the pilot clone, and this document graded it Tier C accordingly.
The 2026-07-11 reincarnation salvage fixed the evidence-practice defect and the row now grades
**A3 — with n still 1**, and with the attempt-1 failing log honestly recorded as the one
non-retained link. The upgrade changed the *warrant*, not the sample size: the thesis has one
live confirmation, not a catch rate.

## 6. What our internal evidence cannot establish

- **n ≈ 1 firing, 1 of 10 tasks merged.** Pilots 2, 3, and 3-v2 run the **same byte-identical
  plan** — one instrument, not independent trials. Only **GL1** ever merged.
- **No clean arm exists.** Pilot-2 was terminated *because* mid-flight machinery evolution
  confounded it (GL1 on pre-I14, everything after on post-I18). Pilot-3-v2 spans I14–I18 then
  I19–I26. **We have never run a controlled A/B.**
- **The self-measuring verifier — the design's distinctive core — has no data.** Empty escapes
  log; no recorded real agent traces (the A6 rig's fixtures are synthetic). Catch-rate is
  **unmeasured**.
- **"Escapes ≈ 0" is unfalsifiable today.** Greenfield, solo operator, pre-production: nothing
  discovers a merged-but-wrong change, so catch-rate reads optimistic exactly where discovery is
  weakest. We cannot claim a low escape rate from this evidence.
- **Concurrency is entirely untested.** All five committed watch items remain unobserved.
- **One operator, one account, one machine, one app.** Every firing is
  `firing-20260705-…-dwijen` on one macOS host, on the **desktop app** — which is *why* the
  statusline rung failed. Environment-specific findings may not generalize.
- **Correctness saturation ≠ model parity** (see §4).
- **Worker liveness remains open.** I29's deadline covers the *observed* 38-minute hang; we have
  no positive evidence of catching a slow-degrading worker before the deadline fires.

## 7. Expiry

| Row | Decay | Recheck when |
|---|---|---|
| Spawn-path probe results (§1, §4) | `vendor-build` | **Every Claude Code build.** Re-run `probe-spawn-portability.js`. Probed at 2.1.45 / 2.1.201. |
| CLI contract breaks (`--json-schema`, `structured_output`, fencing) | `vendor-build` | Every build. |
| Model speed / token efficiency / effort scaling | `model-generation` | Any new model in the lineup. Void for Fable (removed, I28) unless reintroduced. |
| Fable's model-specific weekly cap | `vendor-policy` | Any announced plan change. |
| In-tree self-audit (hooks unregistered, gate skippable) | `our-tree` | Superseded by Phase H — **kept as history, not as current state.** |
| Defect rows in §2 | `our-tree` | All fixed except P3v2-13's liveness ping and the 2026-07-10 concurrency source audit (B-1..B-4) — those stay OPEN by research-mode policy. |
